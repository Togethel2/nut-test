import styled from 'styled-components';

export const HeaderStyled = styled.div`
  .submit{
    padding-top: 15px;
    flex: 0.5
  }

  input[type="submit"]{
    background-color: DodgerBlue;
  }

  .container--flex {
    width: 980px;
    margin: auto;
  }
  
`
